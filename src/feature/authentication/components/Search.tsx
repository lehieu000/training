import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const Search = () => {
    return (
        <View style={styles.wrapperAll}>
            <Text> Lich trinh</Text>
        </View>
    );
};
const styles = StyleSheet.create({
    wrapperAll: {},
});

export default Search;
